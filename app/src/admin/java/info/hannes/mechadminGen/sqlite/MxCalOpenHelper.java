/*
 * Generated by Robotoworks Mechanoid
 */
package info.hannes.mechadminGen.sqlite;

import android.content.ContentValues;
import android.content.Context;
import android.database.sqlite.SQLiteDatabase;

import com.robotoworks.mechanoid.db.SQLiteMigration;

import info.hannes.mechadminGen.sqlite.migrations.DefaultMxCalMigrationV1;
import info.hannes.mechadminGen.sqlite.MxCalContract.DownLoadSite;

public class MxCalOpenHelper extends AbstractMxCalOpenHelper {
    public MxCalOpenHelper(Context context) {
        super(context);
    }

    @Override
    protected SQLiteMigration createMxCalMigrationV1() {

        return new DefaultMxCalMigrationV1() {

            @Override
            public void onAfterUp(SQLiteDatabase db) {
                final ContentValues cvToFilter = DownLoadSite.newBuilder()
                        .setDisplay("MX Cal")
                        .setUrl("ht" + "tp:/" + "/mxcal.com/api/v1/series/search.js")
                        .getValues();
                db.insert(Sources.DOWN_LOAD_SITE, null, cvToFilter);

                final ContentValues cvToFilterR = DownLoadSite.newBuilder()
                        .setDisplay("riderplanet-usa")
                        .setUrl("ht" + "tp:/" + "/ww" + "w.riderplanet-usa.com/")
                        .getValues();
                db.insert(Sources.DOWN_LOAD_SITE, null, cvToFilterR);

                final ContentValues cvToFilter2 = DownLoadSite.newBuilder()
                        .setDisplay("tracksmap.com")
                        .setUrl("ht" + "tp:/" + "/ww" + "w.tracksmap.com/tracksmap2.php?ln=de")
                        .getValues();
                db.insert(Sources.DOWN_LOAD_SITE, null, cvToFilter2);

                final ContentValues cvToFilterBrothers = DownLoadSite.newBuilder()
                        .setDisplay("MX Brothers")
                        .setUrl("http://www.mxbrothers.com/mx-tracks.php?country_zoom=All")
                        .getValues();
                db.insert(Sources.DOWN_LOAD_SITE, null, cvToFilterBrothers);
            }
        };
    }
}
