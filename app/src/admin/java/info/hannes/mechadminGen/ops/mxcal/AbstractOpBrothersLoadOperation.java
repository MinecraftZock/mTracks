/*
 * Generated by Robotoworks Mechanoid
 */
package info.hannes.mechadminGen.ops.mxcal;

import android.content.Intent;
import android.os.Bundle;

import com.robotoworks.mechanoid.Mechanoid;
import com.robotoworks.mechanoid.ops.Operation;
import com.robotoworks.mechanoid.ops.OperationConfiguration;
import com.robotoworks.mechanoid.ops.OperationContext;
import com.robotoworks.mechanoid.ops.OperationResult;
import com.robotoworks.mechanoid.ops.OperationServiceBridge;

public abstract class AbstractOpBrothersLoadOperation extends Operation {
	public static final String ACTION_OP_BROTHERS_LOAD = "info.hannes.mechadmin.ops.mxcal.MxCalService.actions.OP_BROTHERS_LOAD";

	public static final String EXTRA_URL = "info.hannes.mechadmin.ops.mxcal.MxCalService.extras.URL";
	public static final String EXTRA_DOWN_LOAD_ID = "info.hannes.mechadmin.ops.mxcal.MxCalService.extras.DOWN_LOAD_ID";
	public static final String EXTRA_INIT_DOWNLOAD = "info.hannes.mechadmin.ops.mxcal.MxCalService.extras.INIT_DOWNLOAD";

	static class Args {
		public String url;
		public long downLoadId;
		public boolean initDownload;
	}
	
	static class Configuration extends OperationConfiguration {
		@Override 
		public Operation createOperation() {
			return new OpBrothersLoadOperation();
		}
		
		@Override
		public Intent findMatchOnConstraint(OperationServiceBridge bridge, Intent intent) {
			Intent existingRequest = bridge.findPendingRequestByActionWithExtras(AbstractOpBrothersLoadOperation.ACTION_OP_BROTHERS_LOAD, intent.getExtras());
			
			return existingRequest;
			
		}
	}
	
	public static final Intent newIntent(String url, long downLoadId, boolean initDownload) {
		Intent intent = new Intent(ACTION_OP_BROTHERS_LOAD);
		intent.setClass(Mechanoid.getApplicationContext(), MxCalService.class);
		
		Bundle extras = new Bundle();
		extras.putString(EXTRA_URL, url);
		extras.putLong(EXTRA_DOWN_LOAD_ID, downLoadId);
		extras.putBoolean(EXTRA_INIT_DOWNLOAD, initDownload);
		
		intent.putExtras(extras);
		
		return intent;
		
	}

	@Override
	public OperationResult execute(OperationContext context) {
		Args args = new Args();
		
		Bundle extras = context.getIntent().getExtras();
		args.url = extras.getString(EXTRA_URL);
		args.downLoadId = extras.getLong(EXTRA_DOWN_LOAD_ID);
		args.initDownload = extras.getBoolean(EXTRA_INIT_DOWNLOAD);
		
		return onExecute(context, args);
	}
			
	abstract OperationResult onExecute(OperationContext context, Args args);
}
